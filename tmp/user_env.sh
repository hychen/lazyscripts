# this file will contain user environment variables after sudo
# because execute "sudo" on ubuntu will lost all user env variables
# so we need this file to take some environment variables after sudo
# this file will re-write when you execute, generate code in slzs.
